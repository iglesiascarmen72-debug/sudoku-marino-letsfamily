import type { CreatureId } from './types'

export const CREATURE_LABELS: Record<CreatureId, string> = {
  fish: 'pez azul',
  octopus: 'pulpo rosa',
  turtle: 'tortuga verde',
  starfish: 'estrella de mar coral',
}